/*
import { Router } from 'express';
import uiremoteController from '../controllers/UiRemoteController.js';
const router = Router();

router.get('/', uiremoteController.example);

export default router;
*/